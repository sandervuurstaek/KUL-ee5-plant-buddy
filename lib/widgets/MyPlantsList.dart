import 'package:flutter/cupertino.dart';

class MyPlantsLists extends StatelessWidget {
  const MyPlantsLists({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
